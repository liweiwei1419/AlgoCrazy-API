-- 添加多级回复所需字段
ALTER TABLE public.message_board
ADD COLUMN parent_id bigint default 0 not null,
ADD COLUMN level integer default 0 not null,
ADD COLUMN reply_to_nickname varchar(100) default ''::character varying;

-- 添加外键约束（可选）
ALTER TABLE public.message_board
ADD CONSTRAINT fk_message_board_parent
FOREIGN KEY (parent_id) REFERENCES public.message_board(id);

-- 添加索引
CREATE INDEX idx_message_board_parent_id
ON public.message_board (parent_id);

-- 更新字段注释
COMMENT ON COLUMN public.message_board.parent_id IS '父留言ID，0表示顶层留言';
COMMENT ON COLUMN public.message_board.level IS '回复层级：0-顶层，1-一级回复，2-二级回复...';
COMMENT ON COLUMN public.message_board.reply_to_nickname IS '回复对象的昵称';